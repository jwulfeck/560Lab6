To test the hash tables:

make
./prog [desired data file]

To test the example dataset using the alternate values of m, p, and k, change dhFactor in DHHashTable.h to modify p and maxI in DHHashTable.h and QPHashTable.h to modify k.
Change the value passed to the hash table constructors in main.cpp to modify m.